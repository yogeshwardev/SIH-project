# Public HTTPS URL of the deployed CraftLink backend. The Android WebView does
# not share the browser dev proxy, so a packaged phone build needs this value.
VITE_API_BASE=https://api.example.gov.in/api
